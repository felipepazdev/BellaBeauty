const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const startDate = '2026-04-01'; const endDate = '2026-04-30'; const start = new Date(startDate+'T00:00:00'); const end = new Date(endDate+'T23:59:59'); const professionals = await prisma.professional.findMany({ where: { isActive: true }, include: { appointments: { where: { status: 'COMPLETED', date: { gte: start, lte: end } } } } }); console.log(JSON.stringify(professionals, null, 2)); } main().finally(() => prisma.$disconnect());
